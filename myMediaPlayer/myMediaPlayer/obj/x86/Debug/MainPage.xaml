<Page x:ConnectionId='1'
    x:Class="myMediaPlayer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:myMediaPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="using:myMediaPlayer.Converter"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- 声明转换器 -->
        <ResourceDictionary>
            <c:timeLineConverter x:Key="timeLineConverter"/>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:ConnectionId='2' x:Name="myGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Ellipse x:ConnectionId='3' Width="450" Height="450" Margin="75,45,75,45" 
                 Grid.Row="0" x:Name="myMusicPlayer" Visibility="Collapsed"
                 VerticalAlignment="Center" HorizontalAlignment="Center"
                 RenderTransformOrigin="0.5, 0.5">
            <Ellipse.RenderTransform>
                <CompositeTransform />
            </Ellipse.RenderTransform>
            <Ellipse.Fill>
                <ImageBrush ImageSource="Assets/background.jpg"/>
            </Ellipse.Fill>
        </Ellipse>

        <MediaPlayerElement x:ConnectionId='4' x:Name="myMediaPlayer" AreTransportControlsEnabled="False" HorizontalAlignment="Stretch"  Grid.Row="0"/>

        <StackPanel Grid.Row="1">
            <Slider x:ConnectionId='11' Padding="50,0,50,0" x:Name="timeLine"                                                                                                            />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <AppBarButton x:ConnectionId='5' Icon="Play" FontSize="20"                     Label="播放"/>
            <AppBarButton x:ConnectionId='6' Icon="Pause" FontSize="20"                     Label="暂停"/>
            <AppBarButton x:ConnectionId='7' Icon="Stop" FontSize="20"                    Label="停止"/>
            <AppBarButton x:ConnectionId='8' Icon="OpenFile" FontSize="20"                   Label="选择文件"/>
            <AppBarButton Icon="Volume" FontSize="20" Label="音量">
                <AppBarButton.Flyout>
                    <Flyout>
                        <FlipView>
                            <Slider x:ConnectionId='10' x:Name="volumeSlider"                                  Value="1"/>
                        </FlipView>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton x:ConnectionId='9' Icon="FullScreen" FontSize="20"                       Label="全屏"/>
        </StackPanel>
    </Grid>
</Page>

